    
[gcode_macro PRINT_END]
gcode:
    SET_SKEW CLEAR=1  
    M400
    save_zoffset
    SET_IDLE_TIMEOUT TIMEOUT={printer.configfile.settings.idle_timeout.timeout}
    CLEAR_PAUSE
    M106 P2 S0
    M106 P0 S0
    # M106 P3 S0

    M104 S0
    M140 S0
    M141 S0

    M220 S100
    M221 S100
    M84

    DISABLE_ALL_SENSOR
#    SET_GCODE_OFFSET Z=0 MOVE=0
    BED_MESH_CLEAR
    G31
    # CLEAR_LAST_FILE removed (QIDI vendor-only).
    BEEP I=2 DUR=500
    SDCARD_RESET_FILE
    M84
